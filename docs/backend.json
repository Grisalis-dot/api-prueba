{
  "entities": {
    "Asset": {
      "title": "Asset",
      "description": "Representa un activo de IT o nota del sistema.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "titulo": { "type": "string" },
        "cuerpo": { "type": "string" },
        "marca": { "type": "string" },
        "fecha_creacion": { "type": "string", "format": "date-time" }
      },
      "required": ["titulo", "cuerpo", "marca"]
    }
  },
  "auth": {
    "providers": ["anonymous"]
  },
  "firestore": {
    "/assets/{assetId}": {
      "schema": { "$ref": "#/entities/Asset" },
      "description": "Colección principal de activos de IT."
    }
  }
}